using APBD03.Classes;

namespace APBD03;

public class Program
{
    public static void Main(string[] args)
    {
        /*Ship targetShip = new Ship("Shipper", 100, 5, 900);
        Ship originShip = new Ship("Origin", 100, 5, 900);
        
        LContainer lContainer = new LContainer(10, 50, 7, 600);
        LiquidCargo liquidCargo = new LiquidCargo("Milk", false);
        lContainer.LiquidCargo = liquidCargo;
        lContainer.LoadCargo(150);
        
        LContainer lContainer2 = new LContainer(10, 50, 7, 600);
        lContainer2.LiquidCargo = liquidCargo;
        lContainer2.LoadCargo(100);
        
        LContainer lContainer3 = new LContainer(10, 50, 7, 900);
        lContainer3.LiquidCargo = liquidCargo;
        lContainer3.LoadCargo(700);
        
        originShip.AddContainer(lContainer);
        originShip.AddContainer(lContainer3);
        originShip.SwapContainers(lContainer2, lContainer);*/
        
    }
}